#include "main.h"
/**
 * _abs - computes absolute integer value
 *
 * @n: integer to be checked
 */
int _abs(int n)
{
	int n;

	if (n < 0)
	{
		n = (-1) * n;
		printf("%d",n);
	}
	else
	{
		printf("%d",n);
	}
}
