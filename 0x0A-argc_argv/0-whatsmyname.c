#include <stdio.h>
/**
 * main - prints out the name of the program
 * @argc: number of arguments in the program
 * @argv: the array count
 *
 * Return: 0
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
